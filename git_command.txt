echo "# ec2" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/philnane/ec2.git
git push -u origin main

git config --global user.name
git config --global user.address
